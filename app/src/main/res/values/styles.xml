<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorStatusBarTranslucent</item>
    </style>

    <style name="AppTheme.RadioButton.Map" parent="Widget.AppCompat.CompoundButton.RadioButton" >
<!--        <item name="android:button">@android:color/transparent</item>-->
        <item name="android:background">@drawable/radio_button_map_background</item>
        <item name="android:padding">16dp</item>
<!--        <item name="buttonCompat">@null</item>-->
    </style>
</resources>
